<!DOCTYPE html>
<html>
<head>
  <title>GimmeDatBook</title>
  <link href='http://fonts.googleapis.com/css?family=PT+Sans:400,700,400italic,700italic' rel='stylesheet' type='text/css'>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
  <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

</head>
<body>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <i class="fa fa-book"></i>
      <a class="navbar-brand" id="our-logo" href="#">GimmeDatBook</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
    <% if user_signed_in? %>
      <form class="navbar-form navbar-right">
        <!-- <div class="form-group">
          <input type="text" placeholder="Email" class="form-control" autocomplete="off">
        </div>
        <div class="form-group">
          <input type="password" placeholder="Password" class="form-control" autocomplete="off">
        </div> -->
        <%= link_to edit_user_registration_path do %>
          <button type="submit" class="buttons">Account</button>
        <% end %>
        <%= link_to destroy_user_session_path, :method => :delete  do %>
          <button type="submit" class="buttons">Sign Out</button>
        <% end %>
      </form>
      <% flash.each do |name, msg| %>
        <%= content_tag(:p, msg, class: "alerts alert-#{name}") %>
      <% end %>
    </div><!--/.navbar-collapse -->
    <% else %>
    <form class="navbar-form navbar-right">
        <%= link_to new_user_registration_path do %>
          <button type="submit" class="buttons">Sign Up</button>
        <% end %>
        <%= link_to new_user_session_path do %>
          <button type="submit" class="buttons">Sign In</button>
        <% end %>
      </form>
      <% flash.each do |name, msg| %>
        <%= content_tag(:p, msg, class: "alerts alert-#{name}") %>
      <% end %>
    </div><!--/.navbar-collapse -->
    <% end %>
  </div>
</nav>

<%= yield %>

<!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
<!-- Include all compiled plugins (below), or include individual files as needed -->
<script src="bootstrap.min.js"></script>

</body>
</html>
